# Copy the values below from client/.env before running Docker Compose:
#   docker compose --env-file .env.docker up --build
#
# VITE_* values are compiled into the browser bundle. Only use public values here.
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
